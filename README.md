## Сербская таверна — demo frontend (Next.js)

Проект лежит в подпапке `serbian-rest/`.

### Быстрый старт (из этой папки)

```bash
npm install
npm run dev
```

Откройте `http://localhost:3000`.

### Где менять контент

Смотрите `serbian-rest/README.md`.

